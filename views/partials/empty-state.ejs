<div class="empty-state">
  <i data-lucide="<%= icon %>" style="font-size:1.8rem;display:block;margin-bottom:8px;opacity:.7"></i>
  <%= title %>
  <% if (subtitle) { %><div style="margin-top:6px;font-size:0.8rem"><%= subtitle %></div><% } %>
</div>
